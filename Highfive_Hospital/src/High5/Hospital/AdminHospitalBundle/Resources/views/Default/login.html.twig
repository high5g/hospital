{% extends 'High5HospitalAdminHospitalBundle:Default:base.html.twig' %}

{% block body %}
    <div class="login-card">
        <h1>Login</h1><br>
        {% block content %}
            {{ form_start(login_form) }}
            {{ form_widget(login_form) }}
            {{ form_end(login_form) }}
        {% endblock %}
        <div class="login-help"></div>
    </div>
{% endblock %}