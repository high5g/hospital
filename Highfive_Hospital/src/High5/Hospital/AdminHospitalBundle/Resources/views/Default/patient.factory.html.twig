{% extends 'High5HospitalAdminHospitalBundle:Default:base.html.twig' %}

{% block title %}Add Patient{% endblock %}

{% block body %}
    <div class="login-card">
        <h1>Add Patient</h1><br>
        {% block content %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        {% endblock %}
        <div><a href="{{ path('_welcome') }}admin_hospital/homepage">Cancel</a></div>
        <div class="login-help"></div>
    </div>
{% endblock %}